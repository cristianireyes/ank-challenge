import React from 'react';
import IconFA from 'react-native-vector-icons/FontAwesome5';
import { IconProps } from 'react-native-vector-icons/Icon';

const Icon = (props: IconProps) => <IconFA style={{ marginRight: 5 }} {...props} />;
export default Icon;
